# Uncomment the following line to use OpenMP 2.0 features
#OMPFLAG = -DOMPVER2
# Uncomment the following line to use OpenMP 3.0 features
#OMPFLAG = -DOMPVER2 -DOMPVER3

CC = clang
CFLAGS = -fopenmp -O3 -std=gnu99 -lm -I${HOME}/inst/bolt-runtime/include
LDFLAGS = -fopenmp -O3 -lm -L${HOME}/inst/bolt-runtime/lib -lomp
CPP = clang -E
LIBS =
